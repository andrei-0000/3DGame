using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealthSystem : MonoBehaviour
{
/*
    public int health; //vida
	public int maxhealth; //vida maxima
	public event Action OnDie = delegate{};
	public event Action<int,int> OnUpdate = delegate{}; //primer int vida, segon int vida maxima  per a poder fer regla de 3 en el slider
	public void Hurt(int dmg){ // funcio per ferir-se
		health -= dmg;
		OnUpdate.Invoke(health,maxhealth);
		if (health <= 0){ //la meva vida ha acabat,
			OnDie.Invoke();  //invoco el event
		}
	}
	public void Heal(){
		health = maxhealth;
		OnUpdate(health, maxhealth);
	}
	
	public void Heal(int v){
		health += v;
		if(health > maxhealth) Heal();
	}
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
